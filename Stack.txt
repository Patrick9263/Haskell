-- :script C:\Users\Patrick\HaskellProjects\Stack.txt
:! cls

:{
append(value, target_list) =
  value : target_list
:}

:{
helper (n,x,values) =
  let
  n = length values
  first_element = head values
  next_list = tail values
  in
  if n == 0
    then []
    else append(first_element,(helper(n-1,x+1,next_list)))
:}

:{
push(values) =
  helper3(0,0,values)
:}

:{
pop list =
  tail list
:}

example = push([1,2,3,4])
example

pop(example)


putStrLn ""
putStrLn "Process finished with exit code 0"
putStrLn ""
